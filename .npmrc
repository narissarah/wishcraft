# Railway Production npm Configuration
# Suppress update notifications
update-notifier=false

# Use exact versions for reliability
save-exact=true

# Disable fund messages
fund=false

# Disable audit on install
audit=false

# Set registry
registry=https://registry.npmjs.org/

# Prefer offline mode for faster builds
prefer-offline=true

# Use CI mode
progress=false