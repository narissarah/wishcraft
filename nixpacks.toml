[variables]
NODE_VERSION = "18.20.0"

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmd = "npm ci --omit=dev"

[phases.build]
cmd = "npm run build"

[start]
cmd = "npm run start"